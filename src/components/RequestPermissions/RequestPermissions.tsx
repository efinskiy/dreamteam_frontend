import { useEffect } from 'react';
// import { requestPhoneAccess } from '@telegram-apps/sdk-react';
// import { requestPhone } from '@/components/RequestPermissions/utils.ts';

export const RequestPermissions = () => {
    useEffect(() => {
        // if (requestPhoneAccess.isAvailable()) {
        //     requestPhone().then((res) => console.log(res));
        //     // status will be 'sent' | 'cancelled' | string
        // }
        // if (qrScanner.isSupported()) {
        //     qrScanner.open().then((res) => alert(res));
        // }
    }, []);

    return <></>;
};
